package com.learning.java8;

/**
 * Hello world!
 *
 */
public class StreamsApp {
	public static void main(String[] args) {/*
		List<PurchaseOrder> purchaseOrders = PurchaseOrderUtils.createPurchaseOrders();
		List<List<Item>> items = purchaseOrders.stream().filter(order -> equalsIgnoreCase("Company-XYZ", order.getOrderedBy()))
				.map(order -> order.getItems()).distinct().collect(Collectors.toList());
		System.out.println(items);
		String numbers[] = { "1", "2", "3", "4" };
		System.out.println("Stream.empty() : " + Stream.empty().count());
		System.out.println("Stream.of : " + Stream.of("1", "2", "3", "4").count());
		System.out.println("Stream.of : " + Stream.of(numbers).count());
		// Stream.generate(Math::random).forEach(System.out::println);
		Stream<Integer> wholeNumbers = Stream.iterate(1, i -> i + 1);
		// wholeNumbers.forEach(System.out::println);
		try {
			Stream<String> fileLines = Files.lines(Paths.get("F:\\Test.java"));
			fileLines.forEach(System.out::println);
		} catch (IOException e) {
			e.printStackTrace();
		}
	*/}
}
